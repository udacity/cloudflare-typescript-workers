# Cloudflare Typescript Worker Demo

This is the same as the development environment in the parent directory, but
uses the published packages to confirm installation. Use this as an example
configuration.
